name: bughunt

services:
  postgres:
    image: postgres
    container_name: bughunt-postgres
    environment:
      POSTGRES_DB: bughunt
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5434:5432"
    volumes:
      - bughunt_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  bughunt_postgres_data:
